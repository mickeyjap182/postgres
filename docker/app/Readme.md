
## overview

```
conda env export -n std > std.yml
-- conda env create -f=std.yml

conda config --remove channels defaults
conda config --add channels conda-forge
conda config --show channels

/usr/local/bin/conda create -n std -f std.yml
/usr/local/bin/conda env create -f=std.yml

 /Users/yoshitaka.toyama/MyPrograms/miniconda3/bin/conda env create -f=std.yml

 /usr/local/bin/conda create -n std39 python python=3.9 pathlib numpy pandas

 /usr/local/bin/conda env export -n std39 > std39.yml

```


ageia()Ioeopip


u
